# webmcp.fail

A one-sentence dictionary entry. It registers a WebMCP tool (`get_definition`) and serves `/llms.txt`, so an agent never has to read it.

```
npm run dev   # http://webmcp-fail.localhost:1355
```
